---
phase: 03-npx-installer-package-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - test/install.test.js
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - INST-05

must_haves:
  truths:
    - "All installer behaviors are covered by automated tests that run via `npm test`"
    - "Idempotency is verified: running install twice in the same temp environment produces no duplicates"
    - "No-settings-json path is verified: installer exits 0 with warning when settings file is absent"
    - "Summary output is verified: output contains WIRED, ALREADY PRESENT, and COPIED status strings"
  artifacts:
    - path: "test/install.test.js"
      provides: "Automated test suite for bin/install.js"
      contains: "install.test"
  key_links:
    - from: "test/install.test.js"
      to: "bin/install.js"
      via: "child_process.execSync or spawnSync with isolated HOME env var"
      pattern: "install\\.js"
---

<objective>
Write an automated test suite that verifies all five INST requirements by exercising bin/install.js in isolated temp directories.

Purpose: Provides a permanent regression safety net for the installer. Confirms idempotency, hook wiring, skill copy, summary output, and the no-gsd-core warning path — all as fast, headless, repeatable tests.
Output: test/install.test.js (Node built-in test runner format, consistent with package.json test script).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-npx-installer-package-wiring/03-CONTEXT.md
@.planning/phases/03-npx-installer-package-wiring/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| `test/install.test.js` | test file | Node built-in test runner suite — 5 test cases covering INST-01 through INST-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write install.test.js test suite</name>
  <files>test/install.test.js</files>
  <read_first>
    - bin/install.js — understand exact summary strings (WIRED, ALREADY PRESENT, COPIED, Install summary), path constants, and exit codes
    - test/ directory (glob for existing *.test.js files) — confirm existing test file style (Node built-in test runner, assert module)
    - package.json scripts.test — confirm test runner command: `node --test test/*.test.js`
  </read_first>
  <behavior>
    Test 1 (INST-01 — gsd-core absent): Run installer with HOME pointing to empty temp dir (no settings.json). Expect exit code 0 and stdout contains 'WARNING'.
    Test 2 (INST-02 — hook wiring): Run installer with HOME pointing to temp dir containing minimal settings.json { hooks: {} }. After run: settings.json Stop array contains an entry whose hooks[].command includes 'gsd-phase-pacer.js', and SubagentStop array contains an entry whose hooks[].command includes 'gsd-429-guard.js'.
    Test 3 (INST-03 — skill copy): Same setup as Test 2. After run: ~/.claude/plugins/gsd-feature/SKILL.md exists.
    Test 4 (INST-04 — idempotency): Run installer twice with the same temp HOME. After second run: settings.json Stop array has exactly 1 entry matching gsd-phase-pacer, SubagentStop array has exactly 1 entry matching gsd-429-guard, and plugins/gsd-feature/SKILL.md still has its original content (not overwritten).
    Test 5 (INST-05 — summary output): Run installer against a fresh temp HOME with settings.json. Capture stdout. Assert stdout contains 'Install summary', 'WIRED' (at least twice — one for each hook), and 'COPIED' (for the skill). Re-run same dir and assert stdout contains 'ALREADY PRESENT'.
  </behavior>
  <action>
    Create test/install.test.js using Node.js built-in test runner (require('node:test') and require('node:assert')).

    Follow existing test file conventions found in test/ — use test() blocks, not describe/it.

    Each test must:
      1. Create a fresh isolated temp directory using fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-test-')).
      2. Set up the fixture (create or omit ~/.claude/settings.json inside the temp dir).
      3. Run bin/install.js via child_process.spawnSync('node', [path.join(__dirname, '..', 'bin', 'install.js')], { env: { ...process.env, HOME: tmpDir }, encoding: 'utf8' }).
      4. Assert the expected outcomes using assert.strictEqual, assert.ok, or assert.match.
      5. Clean up: fs.rmSync(tmpDir, { recursive: true, force: true }) in a try/finally or after block.

    The USERPROFILE env var should also be set to tmpDir on Windows (spawnSync env: { HOME: tmpDir, USERPROFILE: tmpDir, ...process.env }).

    Test 4 idempotency: after the second run, read settings.json and verify:
      - settings.hooks.Stop.filter(e => e.hooks && e.hooks.some(h => h.command && h.command.includes('gsd-phase-pacer'))).length === 1
      - settings.hooks.SubagentStop.filter(e => e.hooks && e.hooks.some(h => h.command && h.command.includes('gsd-429-guard'))).length === 1

    Test 5 re-run check: second spawnSync call on same tmpDir, capture stdout, assert it contains 'ALREADY PRESENT'.

    No third-party test libraries — only node:test, node:assert, fs, path, os, child_process.

    Do NOT mock fs or child_process — run the actual installer binary against real temp directories.
  </action>
  <verify>
    <automated>node --test test/install.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/install.test.js` exits 0 with all 5 tests passing
    - Test 1 passes: installer exits 0 when settings.json absent, stdout includes 'WARNING'
    - Test 2 passes: after install, settings.json Stop and SubagentStop entries exist with correct command strings
    - Test 3 passes: after install, plugins/gsd-feature/SKILL.md exists in temp dir
    - Test 4 passes: after two installs, Stop and SubagentStop each contain exactly 1 matching entry (no duplicates)
    - Test 5 passes: first run stdout contains 'WIRED' and 'COPIED'; second run stdout contains 'ALREADY PRESENT'
    - `npm test` exits 0 (test/install.test.js is picked up by `node --test test/*.test.js`)
    - test/install.test.js contains no require() calls for packages outside Node.js built-ins and the test runner
  </acceptance_criteria>
  <done>5 tests pass via `node --test test/install.test.js`; npm test exits 0; all INST requirements have automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → tmp directories | Tests write to OS temp; cleaned up after each test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-T1 | Tampering | test temp directories | mitigate | Each test uses a fresh mkdtempSync dir; cleanup in try/finally prevents temp file accumulation |
| T-03-T2 | Denial of Service | test isolation | mitigate | Tests set isolated HOME/USERPROFILE env so they cannot modify the developer's real ~/.claude/settings.json |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; only Node.js built-ins (node:test, node:assert, fs, path, os, child_process) |
</threat_model>

<verification>
- `npm test` exits 0 — all test files pass
- `node --test test/install.test.js` shows 5 passing subtests
- All 5 INST requirements have test coverage (INST-01 through INST-05 mapped to Test 1 through Test 5)
</verification>

<success_criteria>
- 5 automated tests cover INST-01 through INST-05 behaviors
- Tests are fully isolated (no side effects on developer machine)
- npm test passes end to end
</success_criteria>

<output>
Create `.planning/phases/03-npx-installer-package-wiring/03-02-SUMMARY.md` when done.
</output>
