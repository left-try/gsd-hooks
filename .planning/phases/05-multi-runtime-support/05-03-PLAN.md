---
phase: 05-multi-runtime-support
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - hooks/gsd-codex-429-guard.js
  - bin/install.js
  - test/install-codex.test.js
autonomous: true
requirements:
  - MULTI-02
must_haves:
  truths:
    - "npx gsd-hooks wires SubagentStop hook into ~/.codex/hooks.json when Codex config dir exists"
    - "Codex SubagentStop hook reads agent_transcript_path or transcript_path from stdin JSON"
    - "On 429 detection Codex hook activates economy, applies backoff cooldown, emits valid JSON stdout"
    - "Codex install is idempotent and saves hooks-restore.json before first mutation"
    - "Full test suite passes including Claude and Gemini installer tests"
  artifacts:
    - path: hooks/gsd-codex-429-guard.js
      provides: "Codex SubagentStop 429 recovery adapter"
      contains: "SubagentStop"
    - path: bin/install.js
      provides: "installCodex() with snapshot + idempotent SubagentStop wiring"
      contains: "installCodex"
    - path: test/install-codex.test.js
      provides: "Isolated HOME tests for Codex installer"
  key_links:
    - from: bin/install.js
      to: ~/.codex/hooks.json
      via: "installCodex wires SubagentStop command hook"
      pattern: "SubagentStop"
    - from: hooks/gsd-codex-429-guard.js
      to: hooks/gsd-429-guard.js
      via: "runGuard with agent_transcript_path"
      pattern: "runGuard"
---

<objective>
Port 429 recovery and economy activation to Codex via SubagentStop hook and npx installer wiring (MULTI-02).

Purpose: Codex users get the same self-healing 429 behavior as Claude Code without manual hooks.json editing.
Output: `gsd-codex-429-guard.js`, `installCodex()` in `bin/install.js`, automated installer tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-multi-runtime-support/05-CONTEXT.md
@bin/install.js
@hooks/gsd-429-guard.js
@hooks/lib/runtime-hook.js
@test/install.test.js
@test/install-gemini.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codex SubagentStop hook adapter</name>
  <files>hooks/gsd-codex-429-guard.js</files>
  <action>
    Create hooks/gsd-codex-429-guard.js for MULTI-02 (Node.js, no new deps):

    1. Async IIFE when require.main === module; export runCodexGuard for testing.

    2. Read stdin buffer; parse with readStdinJson.

    3. cwd = path.resolve(input.cwd) when string, else process.cwd().

    4. transcriptPath = input.agent_transcript_path || input.transcript_path || '' (prefer agent transcript per Codex SubagentStop schema).

    5. sessionKey = resolveSessionKeyFromInput(input, transcriptPath).

    6. If no transcriptPath: emitJsonStdout({ continue: true }) and exit 0.

    7. await runGuard({ transcriptPath, cwd, sessionKey }).

    8. Codex SubagentStop requires JSON stdout on exit 0: emitJsonStdout({ continue: true, suppressOutput: true }).

    9. All human-readable logs to stderr with [gsd-codex-429-guard] prefix.

    10. On unexpected errors: log to stderr, emitJsonStdout({ continue: true }) and exit 0 — do not crash Codex agent loop with non-zero exit unless unrecoverable (match Claude guard fail-open when no transcript).

    Do NOT use exit code 2 for 429 — economy + cooldown is the recovery mechanism, not subagent continuation control.
  </action>
  <verify>
    <automated>node -e "require('./hooks/gsd-codex-429-guard'); console.log('ok')"</automated>
  </verify>
  <done>Codex adapter loads cleanly; exports runCodexGuard for unit testing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codex installer, integration test, full suite</name>
  <files>bin/install.js, test/install-codex.test.js, test/gsd-codex-429-guard.test.js</files>
  <behavior>
    - When ~/.codex/ absent → WARNING, skip Codex wiring, exit 0
    - Creates ~/.codex/hooks.json with { hooks: {} } when directory exists but file missing
    - First install adds exactly one SubagentStop entry referencing gsd-codex-429-guard
    - Second install: ALREADY PRESENT, no duplicates
    - hooks-restore.json saved before first mutation
    - runCodexGuard with fixture transcript containing '429' creates economy.lock in tmp cwd
  </behavior>
  <action>
    Extend bin/install.js per MULTI-02:

    1. Add installCodex(actions):
       - codexDir = path.join(os.homedir(), '.codex')
       - codexHooksPath = path.join(codexDir, 'hooks.json')
       - codexRestorePath = path.join(codexDir, 'hooks-restore.json')
       - If !fs.existsSync(codexDir): push WARNING "Codex config not found", return
       - readJsonOrEmpty(codexHooksPath) — create { hooks: {} } if file missing (mkdir codexDir if needed)
       - Snapshot original hooks block to hooks-restore.json before mutation
       - Ensure hooks object not array (CR-01 pattern)
       - Wire SubagentStop with empty matcher (Codex ignores matcher for SubagentStop type filtering — use '' matcher, idempotent check command includes 'gsd-codex-429-guard')
       - command: 'node ' + JSON.stringify(absolute path to gsd-codex-429-guard.js)
       - Optional: set timeout 600 in hook config (seconds per Codex docs) — use 600 to allow max 240s cooldown + headroom
       - Write hooks.json only when changed

    2. CLI entry: run installClaude, installGemini, installCodex sequentially; unified printSummary.

    3. Add test/install-codex.test.js (isolated HOME):
       - CODEX-01: no .codex dir → WARNING
       - CODEX-02: wires SubagentStop
       - CODEX-03: idempotent
       - CODEX-04: hooks-restore.json created

    4. Add test/gsd-codex-429-guard.test.js:
       - Unit test runCodexGuard with tmp transcript file containing '429' and stdin JSON payload; assert economy.lock created under tmp cwd
       - Unit test with empty transcript path → no economy.lock

    Run full package test suite as final gate.
  </action>
  <verify>
    <automated>node --test test/*.test.js</automated>
  </verify>
  <done>MULTI-02 complete; all runtime installers pass automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.codex/hooks.json | User hooks config mutated by installer |
| stdin → codex guard | agent_transcript_path content scanned for 429 signals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | hooks.json overwrite | mitigate | hooks-restore.json snapshot before mutation; corrupt JSON abort |
| T-05-08 | Tampering | hook trust (Codex) | transfer | Codex requires user hook trust review — document in SUMMARY; installer uses absolute node paths |
| T-05-09 | Denial of Service | SubagentStop timeout | mitigate | Set hook timeout 600s in hooks.json to exceed 240s max cooldown |
| T-05-10 | Spoofing | agent_transcript_path | accept | Path provided by Codex runtime; same trust model as CLAUDE_TRANSCRIPT_PATH |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `node --test test/install-codex.test.js test/gsd-codex-429-guard.test.js` passes
- `node --test test/*.test.js` passes entire package
</verification>

<success_criteria>
- ROADMAP criteria 3–5 satisfied for Codex
- MULTI-02 requirement covered
- Phase 5 goal achieved: Gemini + Codex users get self-healing economy + 429 recovery via npx install
</success_criteria>

<output>
Create `.planning/phases/05-multi-runtime-support/05-03-SUMMARY.md` when done
</output>
