---
phase: 05-multi-runtime-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/lib/runtime-hook.js
  - hooks/gsd-economy.js
  - hooks/gsd-429-guard.js
  - test/runtime-hook.test.js
  - test/gsd-429-guard.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
must_haves:
  truths:
    - "429 guard core logic accepts transcript path and cwd from callers (not only CLAUDE_TRANSCRIPT_PATH)"
    - "Economy activate/deactivate accept optional cwd so hooks can target project from stdin JSON"
    - "Shared runtime helpers read hook stdin JSON and emit valid JSON stdout for Gemini/Codex contracts"
    - "Claude Code gsd-429-guard behavior unchanged when CLAUDE_TRANSCRIPT_PATH is set"
  artifacts:
    - path: hooks/lib/runtime-hook.js
      provides: "stdin JSON reader, JSON stdout emitter, session key resolver"
      exports: ["readStdinJson", "emitJsonStdout", "resolveSessionKeyFromInput"]
    - path: hooks/gsd-429-guard.js
      provides: "Exported runGuard async function for runtime adapters"
      contains: "runGuard"
    - path: hooks/gsd-economy.js
      provides: "activate/deactivate with optional cwd parameter"
      contains: "function activate"
    - path: test/runtime-hook.test.js
      provides: "Unit tests for runtime-hook helpers"
  key_links:
    - from: hooks/gsd-429-guard.js
      to: hooks/lib/runtime-hook.js
      via: "require for session key resolution when stdin-based"
      pattern: "runtime-hook"
    - from: hooks/gsd-429-guard.js
      to: hooks/gsd-economy.js
      via: "activate({ cwd })"
      pattern: "activate\\("
---

<objective>
Extract shared multi-runtime hook utilities and refactor economy/429 guard to accept cwd + transcript from callers (foundation for MULTI-01 and MULTI-02).

Purpose: Gemini and Codex pass hook context via stdin JSON, not Claude env vars — core logic must be runtime-agnostic before adapters ship.
Output: `hooks/lib/runtime-hook.js`, refactored `gsd-economy.js` and `gsd-429-guard.js`, expanded tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-multi-runtime-support/05-CONTEXT.md
@hooks/gsd-economy.js
@hooks/gsd-429-guard.js
@test/gsd-429-guard.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runtime hook utility module</name>
  <files>hooks/lib/runtime-hook.js, test/runtime-hook.test.js</files>
  <behavior>
    - readStdinJson() parses valid JSON from stdin buffer; returns {} on empty/malformed
    - emitJsonStdout(obj) writes JSON.stringify(obj) + newline to stdout only (no other stdout)
    - resolveSessionKeyFromInput(input, transcriptPath) prefers input.session_id string; falls back to path.basename(path.dirname(transcriptPath)) when transcriptPath set; returns '' when neither available
  </behavior>
  <action>
    Create hooks/lib/runtime-hook.js with three exported functions (CommonJS, no dependencies):

    1. readStdinJson(buffer) — if buffer empty or whitespace, return {}; try JSON.parse; on failure return {}.

    2. emitJsonStdout(payload) — process.stdout.write(JSON.stringify(payload) + '\n').

    3. resolveSessionKeyFromInput(input, transcriptPath) — per D-CONTEXT runtime reference: Gemini/Codex provide session_id on stdin; use that for ADV-02 backoff session identity when present; else reuse basename(dirname(transcriptPath)) matching existing Claude resolveSessionKey behavior.

    Add test/runtime-hook.test.js with node:test unit tests for all three exports. Do NOT add npm dependencies.
  </action>
  <verify>
    <automated>node --test test/runtime-hook.test.js</automated>
  </verify>
  <done>runtime-hook.js exports tested helpers; emitJsonStdout produces single-line JSON only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cwd-aware economy + exported runGuard</name>
  <files>hooks/gsd-economy.js, hooks/gsd-429-guard.js, test/gsd-429-guard.test.js</files>
  <behavior>
    - activate({ cwd }) patches path.join(cwd, '.planning', ...) instead of process.cwd() when cwd provided
    - deactivate({ cwd }) same cwd resolution
    - runGuard({ transcriptPath, cwd, sessionKey }) returns early when no transcript or no rate limit; on 429 activates economy, increments backoff, sleeps cooldown, returns { activated: true, strikeCount, cooldownSecs }
    - Existing CLI entry (require.main) for gsd-429-guard still uses CLAUDE_TRANSCRIPT_PATH and process.cwd()
    - Existing gsd-economy CLI --on/--off still works with process.cwd() default
  </behavior>
  <action>
    Refactor hooks/gsd-economy.js per MULTI-01/02 shared foundation:

    1. Change activate(options) and deactivate(options) signatures to accept optional options object with cwd (default process.cwd()). Replace all path.join(process.cwd(), '.planning', ...) with path.join(baseCwd, '.planning', ...) where baseCwd = options?.cwd || process.cwd(). Preserve require.main === module CLI behavior.

    Refactor hooks/gsd-429-guard.js:

    2. Export async function runGuard({ transcriptPath, cwd, sessionKey }) containing the 429 detection → activate → backoff → log → sleep flow currently in the IIFE. Use cwd || process.cwd() for all .planning paths. Use sessionKey arg when non-empty; else resolveSessionKey(transcriptPath).

    3. Keep module.exports for existing test exports (computeCooldownSecs, detectRateLimit, etc.) plus runGuard.

    4. CLI block: if CLAUDE_TRANSCRIPT_PATH set, call runGuard({ transcriptPath: process.env.CLAUDE_TRANSCRIPT_PATH, cwd: process.cwd() }); else exit 0 immediately (unchanged Claude behavior).

    5. Update test/gsd-429-guard.test.js to cover runGuard with explicit cwd in tmp dir (no CLAUDE_TRANSCRIPT_PATH). Preserve existing ADV-02 tests.

    Do NOT create Gemini/Codex adapter files in this plan — only shared core.
  </action>
  <verify>
    <automated>node --test test/gsd-429-guard.test.js test/runtime-hook.test.js</automated>
  </verify>
  <done>runGuard callable from adapters; economy functions cwd-aware; Claude CLI path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin JSON → runtime-hook | Untrusted hook input from Gemini/Codex runtimes |
| cwd → economy/429 | Hook-supplied cwd determines which .planning/ is patched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | stdin JSON cwd | mitigate | Resolve cwd with path.resolve; reject non-string cwd (fall back to process.cwd()); never chdir process |
| T-05-02 | Tampering | malformed stdin | mitigate | readStdinJson returns {} on parse failure; adapters exit 0 with minimal JSON |
| T-05-03 | Denial of Service | runGuard sleep | accept | Cooldown capped at 240s per ADV-02; same as Claude v1.0 |
| T-05-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `node --test test/runtime-hook.test.js test/gsd-429-guard.test.js` passes
- `node --test test/*.test.js` passes (no regressions in install or pacer tests)
</verification>

<success_criteria>
- Shared foundation ready for Gemini/Codex adapters in 05-02 and 05-03
- Claude Code hook behavior preserved via env-based CLI path
</success_criteria>

<output>
Create `.planning/phases/05-multi-runtime-support/05-01-SUMMARY.md` when done
</output>
