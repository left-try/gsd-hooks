---
phase: 01-economy-rate-limit-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - hooks/gsd-phase-pacer.js
autonomous: true
requirements:
  - PACE-01
  - PACE-02

must_haves:
  truths:
    - "When economy.lock is absent, the pacer sleeps for the configured delay (default 15s, overridable by GSD_PHASE_DELAY_SECS) then exits 0"
    - "When economy.lock is present, the pacer skips the pacing delay entirely and exits 0 immediately — the 429 guard's 60s cooldown already applies"
    - "GSD_PHASE_DELAY_SECS=0 causes the pacer to exit immediately without sleeping, even when economy mode is inactive"
    - "GSD_PHASE_DELAY_SECS=30 causes the pacer to wait 30 seconds before exiting"
    - "The pacer does not touch config.json, economy.lock, or any planning artifact — it is read-only with respect to project state"
  artifacts:
    - path: "hooks/gsd-phase-pacer.js"
      provides: "Stop hook: delay between phases unless economy mode is active"
  key_links:
    - from: "hooks/gsd-phase-pacer.js"
      to: ".planning/economy.lock"
      via: "fs.existsSync — read-only check, never writes"
      pattern: "economy\\.lock"
---

<objective>
Implement the phase pacer Stop hook that introduces a configurable inter-phase delay to prevent burst-induced rate limits during gsd-plan-execute-all chains. When economy mode is already active the delay is skipped since the 429 guard's 60-second cooldown provides sufficient spacing.

Purpose: Smooth out phase-chain throughput without requiring manual intervention. The 15s default gives the Anthropic API enough breathing room between consecutive phase starts.

Output: hooks/gsd-phase-pacer.js
</objective>

<execution_context>
@C:\Users\Ivan\gsd-hooks\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\Ivan\gsd-hooks\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@C:\Users\Ivan\gsd-hooks\.planning\ROADMAP.md
@C:\Users\Ivan\gsd-hooks\.planning\REQUIREMENTS.md
@C:\Users\Ivan\gsd-hooks\.planning\phases\01-economy-rate-limit-layer\01-CONTEXT.md
@C:\Users\Ivan\gsd-hooks\.planning\phases\01-economy-rate-limit-layer\01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gsd-phase-pacer.js — configurable inter-phase delay with economy-mode bypass</name>
  <files>hooks/gsd-phase-pacer.js</files>
  <read_first>
    - C:\Users\Ivan\gsd-hooks\.planning\REQUIREMENTS.md — PACE-01, PACE-02 exact behaviour
    - C:\Users\Ivan\gsd-hooks\.planning\phases\01-economy-rate-limit-layer\01-CONTEXT.md — lock file path, GSD_PHASE_DELAY_SECS env var
    - C:\Users\Ivan\gsd-hooks\hooks\gsd-economy.js — verify lock file path constant used there matches what pacer checks
  </read_first>
  <behavior>
    - Resolve `lockPath` = `path.join(process.cwd(), '.planning', 'economy.lock')`
    - Read delay: parse `parseInt(process.env.GSD_PHASE_DELAY_SECS, 10)` — if NaN or not set, default to `15`; if value is 0 or negative treat as 0 (no sleep)
    - If `fs.existsSync(lockPath)` is true: print "[gsd-phase-pacer] Economy mode active — skipping pacing delay" and exit 0 immediately (PACE-02)
    - If delay is 0: exit 0 immediately with no output
    - Otherwise: print "[gsd-phase-pacer] Pacing delay: {delaySecs}s" then sleep `delaySecs * 1000` ms, then exit 0 (PACE-01)
    - No writes to any file — purely read-only with respect to project state
    - No external imports — only `fs`, `path`, Node builtins
  </behavior>
  <action>
Create `hooks/gsd-phase-pacer.js` as a Node.js CommonJS script with shebang `#!/usr/bin/env node`.

Structure:

1. Imports: `const fs = require('fs'); const path = require('path');`

2. Resolve paths:
   - `lockPath` = `path.join(process.cwd(), '.planning', 'economy.lock')`

3. Read delay:
   ```js
   const rawDelay = parseInt(process.env.GSD_PHASE_DELAY_SECS, 10);
   const delaySecs = isNaN(rawDelay) ? 15 : Math.max(0, rawDelay);
   ```

4. Economy bypass check (PACE-02):
   ```js
   if (fs.existsSync(lockPath)) {
     console.log('[gsd-phase-pacer] Economy mode active — skipping pacing delay');
     process.exit(0);
   }
   ```

5. Zero-delay fast exit:
   ```js
   if (delaySecs === 0) process.exit(0);
   ```

6. Pacing sleep (PACE-01):
   ```js
   console.log(`[gsd-phase-pacer] Pacing delay: ${delaySecs}s`);
   setTimeout(() => process.exit(0), delaySecs * 1000);
   ```

Keep the script simple and linear — no helper functions needed. No async/await — `setTimeout` at top level is sufficient since the process must stay alive for the duration. Do NOT use `sleep` from gsd-economy.js or any shared module.
  </action>
  <verify>
    <automated>node --check hooks/gsd-phase-pacer.js &amp;&amp; echo "Syntax OK"</automated>
  </verify>
  <done>
    - `node --check hooks/gsd-phase-pacer.js` exits 0
    - `GSD_PHASE_DELAY_SECS=0 node hooks/gsd-phase-pacer.js` exits immediately (< 200ms)
    - `GSD_PHASE_DELAY_SECS=1 node hooks/gsd-phase-pacer.js` exits after ~1 second and prints the pacing delay message
    - When run from a directory where `.planning/economy.lock` exists, script exits immediately and prints the economy-mode bypass message regardless of GSD_PHASE_DELAY_SECS
    - No files are created, modified, or deleted by running the pacer
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD_PHASE_DELAY_SECS env var | Integer value from user environment; parsed with parseInt — non-numeric input safely defaults to 15 |
| economy.lock path | Resolved from cwd; read-only existsSync check; no write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | GSD_PHASE_DELAY_SECS | accept | parseInt + Math.max(0, ...) sanitizes all inputs; worst case is 0-second delay (user skips pacing) |
| T-03-02 | Denial of Service | very large GSD_PHASE_DELAY_SECS | accept | User-controlled env var; intentional if set; no upper bound enforcement needed |
| T-03-03 | Spoofing | economy.lock | accept | Checked via existsSync only; script never writes it; attacker creating a fake lock merely suppresses pacing delay — no security impact |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
Run from the project root after execution:

1. `node --check hooks/gsd-phase-pacer.js` — exits 0

2. Zero-delay test:
   ```
   time GSD_PHASE_DELAY_SECS=0 node hooks/gsd-phase-pacer.js
   # real time < 0.2s
   ```

3. Short-delay test:
   ```
   time GSD_PHASE_DELAY_SECS=1 node hooks/gsd-phase-pacer.js
   # prints "[gsd-phase-pacer] Pacing delay: 1s"
   # real time between 0.9s and 1.5s
   ```

4. Economy bypass test:
   ```
   mkdir -p /tmp/gsd-pacer/.planning
   touch /tmp/gsd-pacer/.planning/economy.lock
   cd /tmp/gsd-pacer && time GSD_PHASE_DELAY_SECS=30 node <abs>/hooks/gsd-phase-pacer.js
   # prints bypass message, exits immediately (< 0.2s)
   ```

5. Default delay: run without GSD_PHASE_DELAY_SECS set — should print "Pacing delay: 15s" (kill after confirming message)
</verification>

<success_criteria>
- gsd-phase-pacer.js passes syntax check
- Default delay is 15 seconds when GSD_PHASE_DELAY_SECS is unset
- GSD_PHASE_DELAY_SECS overrides the default to any non-negative integer
- economy.lock present → immediate exit, no delay applied
- No file mutations — purely read-only w.r.t. project state
- No npm dependencies
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| `hooks/gsd-phase-pacer.js` | Hook script | Stop hook; sleeps `GSD_PHASE_DELAY_SECS` (default 15) between phases; skips if economy.lock present |
| `GSD_PHASE_DELAY_SECS` | env var | Configures pacing delay in seconds; default 15; 0 disables pacing |

<output>
Create `.planning/phases/01-economy-rate-limit-layer/01-03-SUMMARY.md` when done
</output>
