---
phase: 06-feature-workflow-enhancements
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - lib/feature-ship.js
  - lib/feature-history.js
  - test/feature-ship.test.js
  - .claude/skills/gsd-feature/SKILL.md
autonomous: true
requirements:
  - FEAT-07
must_haves:
  truths:
    - "User can run /gsd-feature \"desc\" --ship and a PR is created after successful execute+verify"
    - "When verification fails, --ship skips PR creation and reports which criteria failed"
    - "Ship preflight fails gracefully when gh missing, unauthenticated, or no origin remote"
    - "Feature branch feature/{slug} is created when shipping from base branch"
    - "Successful ship updates HISTORY.md PR column for the run's row"
  artifacts:
    - path: lib/feature-ship.js
      provides: "Ship gate, PR body builder, branch/commit helpers for features"
      exports: ["parseFeatureFlags", "canShip", "buildFeaturePrBody", "resolveBaseBranch"]
    - path: test/feature-ship.test.js
      provides: "Automated tests for flag parse, ship gate, PR body generation"
    - path: .claude/skills/gsd-feature/SKILL.md
      provides: "--ship flag parsing and Step 5 ship workflow"
      contains: "--ship"
  key_links:
    - from: .claude/skills/gsd-feature/SKILL.md
      to: lib/feature-ship.js
      via: "Step 5 ship after verification PASS"
      pattern: "canShip|buildFeaturePrBody|gh pr create"
    - from: lib/feature-ship.js
      to: lib/feature-history.js
      via: "updateHistoryPr after successful gh pr create"
      pattern: "updateHistoryPr|appendHistoryEntry"
---

<objective>
Add `/gsd-feature --ship` auto-PR after successful execute+verify (FEAT-07, D-04, D-05, D-06, D-07).

Purpose: Close the feature loop — discuss → plan → execute → verify → PR — without manual `/gsd-ship`.
Output: `lib/feature-ship.js`, tests, SKILL.md flag parsing + Step 5 ship, HISTORY.md PR column update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/workflows/ship.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-feature-workflow-enhancements/06-CONTEXT.md
@.claude/skills/gsd-feature/SKILL.md
@lib/feature-history.js
@.planning/phases/06-feature-workflow-enhancements/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: feature-ship lib — flags, gate, PR body</name>
  <files>lib/feature-ship.js, lib/feature-history.js, test/feature-ship.test.js</files>
  <behavior>
    - parseFeatureFlags('"add dark mode" --ship --economy') → { description: 'add dark mode', ship: true, economy: true }
    - parseFeatureFlags('"only economy" --economy') → ship false
    - canShip({ overallPass: true, summaryStatus: 'complete' }) → { ok: true }
    - canShip({ overallPass: false, failedCriteria: ['criterion 2'] }) → { ok: false, reason includes failed criteria }
    - buildFeaturePrBody reads SUMMARY/CONTEXT/PLAN from feature dir → markdown with Summary, Changes, Verification sections
    - resolveBaseBranch returns main when no git config (test with mock or default fallback)
    - updateHistoryPr replaces — in last matching slug row or appends pr to most recent row for slug
  </behavior>
  <action>
    Per FEAT-07 and D-04/D-06/D-07, create lib/feature-ship.js (CommonJS, no new npm deps).

    1. parseFeatureFlags(argString): strip whitespace; detect --ship and --economy tokens (word boundaries); remove tokens from string; return { description, ship, economy } booleans.

    2. canShip({ overallPass, summaryStatus, failedCriteria }): return { ok: boolean, reason?: string }. ok only when overallPass===true AND summaryStatus==='complete'. reason lists failed criteria when blocked.

    3. buildFeaturePrBody({ slug, description, featureDir }): read PLAN.md, CONTEXT.md, SUMMARY.md if present (fs readFileSync, graceful empty). Emit markdown:
       - Title line suggestion: `Feature: {description}` (truncate 72)
       - ## Summary — description + slug + status from SUMMARY
       - ## Changes — files from SUMMARY ## Files Created/Modified
       - ## Verification — copy verification table from SUMMARY
       Keep under reasonable size; escape nothing that breaks markdown.

    4. resolveBaseBranch(cwd): try read .planning/config.json git.base_branch; else return 'main' (git symbolic-ref fallback documented for executor shell step).

    5. featureBranchName(slug): return `feature/${slug}` per D-05.

    6. In lib/feature-history.js add updateHistoryPr(cwd, slug, prUrl): find last table row where slug column matches; replace trailing `| — |` or `| —` with `| {url} |`. If no row found, no-op. Export alongside appendHistoryEntry.

    7. test/feature-ship.test.js: unit tests for parseFeatureFlags, canShip cases, buildFeaturePrBody with fixture tmp files. test updateHistoryPr in feature-history tests or feature-ship tests.

    Do NOT shell out to gh in unit tests.
  </action>
  <verify>
    <automated>node --test test/feature-ship.test.js test/feature-history.test.js</automated>
  </verify>
  <done>feature-ship.js and updateHistoryPr tested; ship gate and PR body generation deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md --ship flag and Step 5 ship workflow</name>
  <files>.claude/skills/gsd-feature/SKILL.md</files>
  <action>
    Per FEAT-07, D-04, D-05, D-06, D-07, update .claude/skills/gsd-feature/SKILL.md.

    1. Step 1 Bootstrap — replace --economy-only parsing with parseFeatureFlags pattern:
       - Parse --ship and --economy (D-07); set SHIP_FLAG and ECONOMY_FLAG
       - Update usage to show: `/gsd-feature "description" [--ship] [--economy]`
       - Banner includes `Ship: {on|off}`

    2. Add Step 5 — Ship (runs only when SHIP_FLAG=true, after Step 4b history append):

    a. Gate: call canShip from lib/feature-ship with verification Overall and SUMMARY status. If !ok: print `◆ Ship skipped: {reason}` and STOP — no PR, no git push. Per ROADMAP success criterion 2.

    b. Preflight (adapt ship.md):
       - which gh && gh auth status — if fail, print setup instructions and skip
       - git remote -v — require origin
       - git status --short — if dirty, git add -A && git commit -m "feat({SLUG}): {description truncated 72}"

    c. Branch: CURRENT=$(git branch --show-current); BASE=$(resolveBaseBranch); if CURRENT===BASE, git checkout -b feature/{SLUG}

    d. Push: git push -u origin HEAD (report errors, skip PR on failure)

    e. PR body: write temp file from buildFeaturePrBody; gh pr create --title "Feature: {description}" --body-file {tmp} --base {BASE}

    f. On success: updateHistoryPr with PR URL; print banner with PR number and URL

    3. If SHIP_FLAG=false, Step 5 is skipped entirely (existing flow unchanged).

    4. Security: never pass raw DESCRIPTION to shell unquoted; use body-file for gh; slug already sanitized.

    5. Run node --test test/*.test.js full suite.
  </action>
  <verify>
    <automated>node --test test/*.test.js</automated>
  </verify>
  <done>SKILL.md documents --ship end-to-end; failed verify blocks PR with explicit reason; successful ship creates PR and updates HISTORY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user args → git/gh | Feature description and flags drive branch name and commit message |
| feature artifacts → PR body | PLAN/CONTEXT/SUMMARY content published to GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | git commit message | mitigate | Use sanitized SLUG only; description truncated; no command substitution |
| T-06-05 | Information Disclosure | PR body | accept | User explicitly requested --ship; content is their feature artifacts |
| T-06-06 | Elevation | gh pr create | mitigate | Requires user-authenticated gh; preflight checks auth before create |
| T-06-07 | Spoofing | origin remote | mitigate | Verify origin exists; report URL from gh output not user input |
| T-06-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- node --test test/feature-ship.test.js test/feature-history.test.js passes
- node --test test/*.test.js passes
- SKILL.md contains --ship flag docs, Step 5 ship flow, and skip-on-fail behavior
</verification>

<success_criteria>
- FEAT-07 satisfied: --ship creates PR after PASS verification; skips with reason on FAIL
- HISTORY.md PR column updated on successful ship
- No writes to ROADMAP.md, STATE.md, or .planning/phases/
</success_criteria>

<output>
Create `.planning/phases/06-feature-workflow-enhancements/06-02-SUMMARY.md` when done
</output>
