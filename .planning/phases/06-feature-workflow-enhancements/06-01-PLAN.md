---
phase: 06-feature-workflow-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/feature-history.js
  - test/feature-history.test.js
  - .claude/skills/gsd-feature/SKILL.md
autonomous: true
requirements:
  - FEAT-08
must_haves:
  truths:
    - "Each /gsd-feature run appends one row to .planning/features/HISTORY.md with slug, date, and completion status"
    - "User can open HISTORY.md and see all past feature runs without opening individual feature directories"
    - "History append happens after verification completes, reflecting final complete or incomplete status"
    - "Re-running append for the same slug on the same day adds a new row (no silent overwrite of prior runs)"
  artifacts:
    - path: lib/feature-history.js
      provides: "Append-only feature history table helpers"
      exports: ["appendHistoryEntry", "formatHistoryRow", "ensureHistoryFile"]
    - path: .planning/features/HISTORY.md
      provides: "Per-project feature run log (created on first run)"
      contains: "| Date | Slug | Status | Description | PR |"
    - path: test/feature-history.test.js
      provides: "Automated coverage for append, create, and slug safety"
  key_links:
    - from: .claude/skills/gsd-feature/SKILL.md
      to: lib/feature-history.js
      via: "Step 4 calls appendHistoryEntry after SUMMARY.md written"
      pattern: "appendHistoryEntry|HISTORY\\.md"
    - from: lib/feature-history.js
      to: .planning/features/HISTORY.md
      via: "fs appendFileSync with formatted markdown table row"
      pattern: "features/HISTORY"
---

<objective>
Implement per-project feature history logging (FEAT-08, D-01, D-02, D-03).

Purpose: Give users a single scan-friendly log of all `/gsd-feature` runs without opening each feature folder.
Output: `lib/feature-history.js`, tests, and SKILL.md Step 4 integration that appends after verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-feature-workflow-enhancements/06-CONTEXT.md
@.claude/skills/gsd-feature/SKILL.md
@test/gsd-phase-pacer.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: feature-history lib with append helpers</name>
  <files>lib/feature-history.js, test/feature-history.test.js</files>
  <behavior>
    - ensureHistoryFile on empty dir creates HISTORY.md with header row and separator
    - appendHistoryEntry adds row: date, slug, status, description (max 80 chars), pr default —
    - Second append preserves prior rows and adds new row at end
    - Slug with path traversal chars rejected (throws or returns error — consistent API)
    - Description pipes in markdown table escaped (| → \|)
    - Missing .planning/features/ directory created before first write
  </behavior>
  <action>
    Per FEAT-08 and D-01/D-02/D-03, create lib/feature-history.js (CommonJS, no new npm deps).

    1. Export constants: HISTORY_REL_PATH = path.join('.planning', 'features', 'HISTORY.md'); TABLE_HEADER markdown with columns Date | Slug | Status | Description | PR |

    2. sanitizeSlug(slug): reject if empty, contains '..' or '/', or chars outside [a-z0-9-]. Throw Error with clear message (matches SKILL slug rules).

    3. escapeTableCell(value): string coerce, replace | with \|, strip newlines/carriage returns.

    4. truncateDescription(desc, max=80): trim, truncate with ellipsis if needed.

    5. ensureHistoryFile(cwd): resolve HISTORY path under cwd; mkdirSync .planning/features recursive; if file missing write header + separator rows only.

    6. formatHistoryRow({ date, slug, status, description, pr }): return single markdown table row string ending with newline. status must be 'complete' or 'incomplete' only.

    7. appendHistoryEntry(cwd, entry): sanitize slug; ensure file; append formatted row via appendFileSync. entry.date defaults to new Date().toISOString().slice(0,10). entry.pr defaults to '—'.

    8. In test/feature-history.test.js use mkdtemp isolated dirs. Test create, double append, description truncation, pipe escape, invalid slug rejection. Run RED then GREEN.
  </action>
  <verify>
    <automated>node --test test/feature-history.test.js</automated>
  </verify>
  <done>feature-history.js exports tested append API; all feature-history tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire history append into gsd-feature SKILL Step 4</name>
  <files>.claude/skills/gsd-feature/SKILL.md</files>
  <action>
    Per FEAT-08 and D-03, update .claude/skills/gsd-feature/SKILL.md Step 4 (after SUMMARY.md write, before final banner).

    1. Add a "Step 4b — Record history" subsection that runs after SUMMARY.md is written:

    - Invoke history append via Node one-liner using the package lib (executor resolves package root from repo):
      node -e "const h=require('./lib/feature-history'); h.appendHistoryEntry(process.cwd(), { slug: SLUG, status: STATUS_FROM_SUMMARY, description: DESCRIPTION, pr: '—' });"
      Map STATUS_FROM_SUMMARY from SUMMARY frontmatter/body: 'complete' when Overall PASS and user did not Stop with incomplete; 'incomplete' otherwise.

    - Print: `◆ History updated: .planning/features/HISTORY.md`

    2. Document HISTORY.md in skill notes: location, column meanings, that PR column is updated by --ship in plan 06-02 (placeholder — for now always —).

    3. Update HARD CONSTRAINT note: allowed writes are `.planning/features/{slug}/*` AND `.planning/features/HISTORY.md` only — still never ROADMAP.md, STATE.md, or `.planning/phases/`.

    4. Do NOT add --ship logic in this plan (FEAT-07 is 06-02).

    5. Run full test suite to ensure no regressions.
  </action>
  <verify>
    <automated>node --test test/*.test.js</automated>
  </verify>
  <done>SKILL.md documents Step 4b history append; FEAT-08 integration path is explicit for executors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slug → filesystem | Untrusted feature description-derived slug used in paths |
| description → HISTORY.md | User-provided text rendered into markdown table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | lib/feature-history.js slug | mitigate | sanitizeSlug rejects .., /, and invalid chars before any path join |
| T-06-02 | Tampering | HISTORY.md content | mitigate | escapeTableCell on all column values; no shell execution of description |
| T-06-03 | Denial of Service | unbounded HISTORY.md | accept | Append-only log; user-owned project file; no cap required by spec |
| T-06-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- node --test test/feature-history.test.js passes
- node --test test/*.test.js passes (no regressions)
- Manual: appendHistoryEntry in temp dir produces readable HISTORY.md table with expected columns
</verification>

<success_criteria>
- FEAT-08 satisfied: every feature run appends slug, date, status to project history log
- User can read `.planning/features/HISTORY.md` for full run list without opening feature subdirs
</success_criteria>

<output>
Create `.planning/phases/06-feature-workflow-enhancements/06-01-SUMMARY.md` when done
</output>
