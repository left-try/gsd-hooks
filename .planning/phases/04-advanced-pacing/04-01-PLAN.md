---
phase: 04-advanced-pacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-phase-pacer.js
  - test/gsd-phase-pacer.test.js
autonomous: true
requirements:
  - ADV-01
must_haves:
  truths:
    - "User can set hooks.phase_delay_secs in .planning/config.json and phase pacing uses that value"
    - "hooks.phase_delay_secs overrides GSD_PHASE_DELAY_SECS when present in config"
    - "hooks.phase_delay_secs set to 0 disables pacing for that project (immediate exit, no delay message)"
    - "When hooks.phase_delay_secs is absent, GSD_PHASE_DELAY_SECS env behavior is unchanged (default 15s)"
    - "economy.lock still bypasses pacing delay regardless of config or env"
  artifacts:
    - path: hooks/gsd-phase-pacer.js
      provides: "Config-aware phase delay resolution with env fallback"
      contains: "phase_delay_secs"
    - path: test/gsd-phase-pacer.test.js
      provides: "Automated coverage for config override, disable, and fallback paths"
  key_links:
    - from: hooks/gsd-phase-pacer.js
      to: .planning/config.json
      via: "read hooks.phase_delay_secs from JSON at startup"
      pattern: "hooks\\.phase_delay_secs|config\\.json"
    - from: hooks/gsd-phase-pacer.js
      to: .planning/economy.lock
      via: "existsSync check before delay"
      pattern: "economy\\.lock"
---

<objective>
Implement per-project phase pacing via `hooks.phase_delay_secs` in `.planning/config.json` (ADV-01).

Purpose: Let users tune burst smoothing per project without global env vars; config hot-reloads because gsd-core watches `.planning/config.json`.
Output: Updated `gsd-phase-pacer.js` with config-first delay resolution and expanded test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@hooks/gsd-phase-pacer.js
@hooks/gsd-economy.js
@test/gsd-phase-pacer.test.js
</context>

## Artifacts this phase produces

| Artifact | Plan | Description |
|----------|------|-------------|
| `hooks/gsd-phase-pacer.js` | 04-01 | Reads `hooks.phase_delay_secs` from config; overrides env when set |
| `test/gsd-phase-pacer.test.js` | 04-01 | Tests config override, config=0 disable, env fallback, economy.lock bypass |
| `hooks/gsd-429-guard.js` | 04-02 | Session-scoped exponential backoff (60s → 120s → 240s) |
| `test/gsd-429-guard.test.js` | 04-02 | Tests backoff ladder, session reset, and 429 detection wiring |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config-first delay resolution in gsd-phase-pacer</name>
  <read_first>
    - hooks/gsd-phase-pacer.js — current env-only delay and economy.lock bypass
    - hooks/gsd-economy.js — readJsonOrEmpty pattern for safe config reads
    - .planning/config.json — existing hooks section shape (`hooks.context_warnings`)
    - test/gsd-phase-pacer.test.js — spawnSync child-process test harness
  </read_first>
  <files>hooks/gsd-phase-pacer.js, test/gsd-phase-pacer.test.js</files>
  <behavior>
    - Config hooks.phase_delay_secs=5 with GSD_PHASE_DELAY_SECS=30 → uses 5s (message says "5s")
    - Config hooks.phase_delay_secs=0 → exits immediately (&lt;500ms), no pacing stdout
    - Config absent, GSD_PHASE_DELAY_SECS=2 → uses 2s (env fallback unchanged)
    - Config hooks.phase_delay_secs invalid (string "abc") → falls back to env/default like non-numeric env
    - economy.lock present → bypass message, immediate exit regardless of config value
  </behavior>
  <action>
    Per ADV-01, add config reading to hooks/gsd-phase-pacer.js before applying delay.

    1. Add a local readJsonOrEmpty(filePath) helper (same safe-parse pattern as hooks/gsd-economy.js readJsonOrEmpty — return {} on missing/malformed; do NOT require gsd-economy to avoid coupling).

    2. Add resolveDelaySecs(cwd) that returns a non-negative integer:
       - Read path.join(cwd, '.planning', 'config.json')
       - If config.hooks.phase_delay_secs is present (use Object.prototype.hasOwnProperty.call on hooks object): parse as integer; if NaN treat as unset and fall through to env
       - When config value is present and numeric: return Math.max(0, parsed) — this OVERRIDES GSD_PHASE_DELAY_SECS entirely (including 0 to disable)
       - When config value absent or hooks key missing: keep existing env logic (parseInt GSD_PHASE_DELAY_SECS, default 15, Math.max(0, ...))

    3. Replace top-level rawDelay/delaySecs with resolveDelaySecs(process.cwd()). Preserve existing economy.lock early-exit (PACE-02) and zero-delay silent exit behavior.

    4. Do NOT write or modify config.json from the pacer — read-only. Do NOT add npm dependencies.

    5. In test/gsd-phase-pacer.test.js, add helper writeConfig(tmpDir, hooksObj) that writes .planning/config.json with a hooks section. Add tests BEFORE implementing (RED) then implement (GREEN):
       - config phase_delay_secs=1 overrides env 30 → message "Pacing delay: 1s", duration ~1s
       - config phase_delay_secs=0 → immediate exit even when env is 30
       - config absent → existing env tests still pass (no regression)
       - config phase_delay_secs=1 + economy.lock → bypass message, immediate exit
  </action>
  <acceptance_criteria>
    - resolveDelaySecs is the single source of truth for delay seconds in gsd-phase-pacer.js
    - Numeric hooks.phase_delay_secs in config always wins over GSD_PHASE_DELAY_SECS when the key exists
    - hooks.phase_delay_secs: 0 disables pacing (exit 0, no delay log line)
    - Malformed config.json does not crash the hook; falls back to env/default
    - economy.lock bypass unchanged from v1.0 behavior
    - All new tests pass alongside existing 7 pacer tests
  </acceptance_criteria>
  <verify>
    <automated>node --test test/gsd-phase-pacer.test.js</automated>
  </verify>
  <done>gsd-phase-pacer reads hooks.phase_delay_secs from config, overrides env when set, and all pacer tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Regression guard and config read safety</name>
  <read_first>
    - hooks/gsd-phase-pacer.js — Task 1 implementation
    - test/gsd-phase-pacer.test.js — full test file after Task 1
  </read_first>
  <files>test/gsd-phase-pacer.test.js</files>
  <action>
    Per ADV-01 edge cases, harden test coverage without changing production behavior unless a gap is found.

    1. Add test: malformed config.json (invalid JSON text) with GSD_PHASE_DELAY_SECS=1 → hook still runs, uses env value 1s (proves readJsonOrEmpty safety).

    2. Add test: config has hooks object but no phase_delay_secs key, env=1 → uses env (proves "only override when key present").

    3. Add test: pacer still does not create or modify files in .planning/ when config is present (extend existing file-mutation test to write a config first).

    4. Run full package test suite to ensure no cross-file regressions.

    If Task 1 left any failing edge case, fix gsd-phase-pacer.js minimally — do not expand scope beyond ADV-01.
  </action>
  <acceptance_criteria>
    - Malformed config and missing-key configs have explicit automated tests
    - node --test test/*.test.js passes with zero failures
    - Pacer remains read-only on disk (no config writes)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/*.test.js</automated>
  </verify>
  <done>ADV-01 fully covered with edge-case tests; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → pacer | Untrusted project-local JSON read at hook invocation |
| env → pacer | Process environment may be set by user or installer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | hooks/gsd-phase-pacer.js config read | mitigate | readJsonOrEmpty returns {} on malformed JSON; parseInt + isNaN guard; Math.max(0, n) prevents negative sleep |
| T-04-02 | Denial of Service | phase_delay_secs value | mitigate | Cap not required by spec but Math.max(0, n) prevents negative; document that very large values are user-configured (acceptable) |
| T-04-03 | Information Disclosure | config.json path | accept | Hook only reads project-local .planning/config.json already used by gsd-core; no secrets logged |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `node --test test/gsd-phase-pacer.test.js` passes all cases including config override and disable
- `node --test test/*.test.js` passes (no regressions in install tests)
- Manual spot-check: set `"hooks": { "phase_delay_secs": 3 }` in .planning/config.json, run pacer, observe "Pacing delay: 3s"
</verification>

<success_criteria>
- ADV-01 requirement satisfied: per-project hooks.phase_delay_secs controls pacing and overrides env when set
- All existing pacer behaviors preserved (default 15s, economy.lock bypass, zero-disable)
</success_criteria>

<output>
Create `.planning/phases/04-advanced-pacing/04-01-SUMMARY.md` when done
</output>
