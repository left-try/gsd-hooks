---
phase: 04-advanced-pacing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-429-guard.js
  - test/gsd-429-guard.test.js
autonomous: true
requirements:
  - ADV-02
must_haves:
  truths:
    - "First 429 in a session triggers 60s cooldown (existing behavior preserved)"
    - "Second 429 in the same session triggers 120s cooldown"
    - "Third and subsequent 429s in the same session trigger 240s cooldown (capped)"
    - "New session resets backoff to 60s — strike count does not carry across sessions"
    - "Backoff state is NOT stored in economy.lock (separate session-scoped mechanism)"
    - "Economy activation and rate-limit-log.json append still occur on every 429"
  artifacts:
    - path: hooks/gsd-429-guard.js
      provides: "Session-scoped exponential backoff with exported test helpers"
      contains: "computeCooldownSecs"
    - path: test/gsd-429-guard.test.js
      provides: "Automated backoff ladder and session-reset tests"
    - path: .planning/rate-limit-backoff.json
      provides: "Runtime session backoff state (created at hook run, not committed)"
      contains: "sessionKey"
  key_links:
    - from: hooks/gsd-429-guard.js
      to: .planning/rate-limit-backoff.json
      via: "read/write strike count keyed by sessionKey"
      pattern: "rate-limit-backoff"
    - from: hooks/gsd-429-guard.js
      to: hooks/gsd-economy.js
      via: "activate() on 429"
      pattern: "activate\\(\\)"
    - from: hooks/gsd-429-guard.js
      to: CLAUDE_TRANSCRIPT_PATH
      via: "sessionKey derived from transcript path"
      pattern: "CLAUDE_TRANSCRIPT_PATH"
---

<objective>
Implement session-scoped exponential backoff for repeated 429s in hooks/gsd-429-guard.js (ADV-02).

Purpose: Reduce repeated rate-limit hammering within a single Claude Code session while resetting cleanly when a new session starts.
Output: Refactored gsd-429-guard.js with backoff state file, pure helper exports for tests, and new test/gsd-429-guard.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@hooks/gsd-429-guard.js
@hooks/gsd-economy.js
@test/gsd-phase-pacer.test.js
</context>

## Artifacts this phase produces

| Artifact | Plan | Description |
|----------|------|-------------|
| `hooks/gsd-phase-pacer.js` | 04-01 | Reads `hooks.phase_delay_secs` from config; overrides env when set |
| `test/gsd-phase-pacer.test.js` | 04-01 | Tests config override, config=0 disable, env fallback, economy.lock bypass |
| `hooks/gsd-429-guard.js` | 04-02 | Session-scoped exponential backoff (60s → 120s → 240s) |
| `test/gsd-429-guard.test.js` | 04-02 | Tests backoff ladder, session reset, and 429 detection wiring |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backoff helpers and session-scoped state file</name>
  <read_first>
    - hooks/gsd-429-guard.js — detectRateLimit, appendLog, fixed 60_000ms sleep
    - hooks/gsd-economy.js — activate() export and lock file semantics (do NOT reuse economy.lock for backoff)
    - test/gsd-phase-pacer.test.js — spawnSync + tmp dir patterns for hook tests
    - .planning/STATE.md — v1.0 decision: pacing defers to economy.lock; backoff is separate concern
  </read_first>
  <files>hooks/gsd-429-guard.js, test/gsd-429-guard.test.js</files>
  <behavior>
    - computeCooldownSecs(1) === 60, computeCooldownSecs(2) === 120, computeCooldownSecs(3) === 240, computeCooldownSecs(99) === 240
    - resolveSessionKey('/path/to/session-abc/transcript.jsonl') === 'session-abc' (basename of parent dir)
    - First 429 write: rate-limit-backoff.json { sessionKey, strikeCount: 1 }
    - Second 429 same sessionKey: strikeCount increments to 2
    - 429 with different sessionKey: strikeCount resets to 1 (not 2)
  </behavior>
  <action>
    Per ADV-02, refactor hooks/gsd-429-guard.js to track per-session strike count and scale cooldown.

    1. Create test/gsd-429-guard.test.js FIRST (RED). Test pure exports via require('../hooks/gsd-429-guard') — the hook IIFE must not run on require (guard with require.main === module pattern matching gsd-economy.js).

    2. Export these pure/testable functions from gsd-429-guard.js:
       - BACKOFF_MS_LADDER constant or computeCooldownSecs(strikeCount): returns seconds 60 | 120 | 240 (cap at 240 for strikeCount >= 3)
       - resolveSessionKey(transcriptPath): return path.basename(path.dirname(transcriptPath)); if transcriptPath falsy return ''
       - readBackoffState(backoffPath): returns { sessionKey: string, strikeCount: number } defaulting to { sessionKey: '', strikeCount: 0 }
       - writeBackoffState(backoffPath, state): writes pretty JSON to .planning/rate-limit-backoff.json
       - incrementStrike(backoffPath, sessionKey): if stored sessionKey !== sessionKey, reset strikeCount to 0; increment; persist; return new strikeCount

    3. Backoff state file path: path.join(process.cwd(), '.planning', 'rate-limit-backoff.json') — NOT economy.lock. Document in code comment that this file is session-scoped via sessionKey and may be stale across sessions (reset on key mismatch).

    4. In the main IIFE after 429 detection:
       - const sessionKey = resolveSessionKey(transcriptPath)
       - const strikeCount = incrementStrike(backoffPath, sessionKey)
       - const cooldownSecs = computeCooldownSecs(strikeCount)
       - Keep activate() and appendLog() calls unchanged in sequence
       - Update console.log to include strikeCount and cooldownSecs: e.g. "cooling down 120s (strike 2/3+)"
       - await sleep(cooldownSecs * 1000) instead of fixed 60_000

    5. Extend appendLog entry with optional fields: strikeCount, cooldownSecs (backward compatible — still valid JSON array).

    6. Do NOT persist backoff in economy.lock. Do NOT add npm dependencies.

    7. For integration tests that spawn the hook process, support env GSD_BACKOFF_TEST_MS — when set to a positive integer, sleep that many ms instead of cooldownSecs * 1000 (test-only shortcut; not documented for end users). Guard behind process.env.GSD_BACKOFF_TEST_MS parse.
  </action>
  <acceptance_criteria>
    - computeCooldownSecs implements exact ladder: 60 → 120 → 240 capped
    - Session identity derived from CLAUDE_TRANSCRIPT_PATH parent directory name
    - New session (different sessionKey) resets strike count to 1 on first 429
    - rate-limit-backoff.json used for strike persistence within same session (separate from economy.lock)
    - require('../hooks/gsd-429-guard') does not trigger sleep or process.exit
    - Unit tests for pure functions pass before integration tests
  </acceptance_criteria>
  <verify>
    <automated>node --test test/gsd-429-guard.test.js</automated>
  </verify>
  <done>Backoff ladder and session-scoped state implemented with exported testable helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for 429 guard backoff wiring</name>
  <read_first>
    - hooks/gsd-429-guard.js — Task 1 exports and main IIFE
    - test/gsd-429-guard.test.js — unit tests from Task 1
    - hooks/gsd-economy.js — economy.lock path for asserting activate side effects
  </read_first>
  <files>test/gsd-429-guard.test.js</files>
  <action>
    Per ADV-02 end-to-end behavior, add spawn-based integration tests mirroring gsd-phase-pacer.test.js style.

    1. Add runGuard(cwd, envOverrides) helper: spawnSync node on gsd-429-guard.js with cwd and env; return { status, stdout, durationMs }.

    2. Add makeTmpDirWithTranscript(sessionId, content) helper: creates .planning/, writes a fake transcript at tmp/transcripts/{sessionId}/transcript.jsonl with 429 signal string in content.

    3. Integration test A — first 429 in session:
       - Set CLAUDE_TRANSCRIPT_PATH to fake transcript containing '429'
       - Set GSD_BACKOFF_TEST_MS=100 (or similar small value)
       - Run guard; assert stdout mentions 60s cooldown and strike 1
       - Assert .planning/rate-limit-backoff.json exists with strikeCount 1
       - Assert economy.lock created (activate ran)
       - Assert rate-limit-log.json has new entry

    4. Integration test B — second 429 same session:
       - Pre-seed rate-limit-backoff.json with same sessionKey, strikeCount 1
       - Run guard with same CLAUDE_TRANSCRIPT_PATH session dir
       - Assert stdout mentions 120s cooldown and strike 2
       - Assert backoff file now strikeCount 2

    5. Integration test C — new session resets:
       - Pre-seed backoff with sessionKey 'old-session', strikeCount 5
       - Run guard with CLAUDE_TRANSCRIPT_PATH pointing to 'new-session' dir
       - Assert strikeCount becomes 1 (not 6) and cooldown message shows 60s

    6. Integration test D — no 429 in transcript:
       - Transcript without rate limit signals → immediate exit, no backoff file write, duration &lt; 500ms

    7. Run full suite: node --test test/*.test.js

    Mock or isolate gsd-economy side effects in tmp dirs only — never touch real ~/.claude or project .planning outside tmp.
  </action>
  <acceptance_criteria>
    - Spawn tests prove 60s → 120s ladder and session reset without waiting real 60s (GSD_BACKOFF_TEST_MS)
    - No-429 path exits fast with no backoff mutation
    - Full package test suite passes
    - economy.lock still created on 429 (RATE-02 preserved)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/*.test.js</automated>
  </verify>
  <done>ADV-02 fully verified: exponential backoff within session, reset across sessions, existing 429 recovery preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript → guard | CLAUDE_TRANSCRIPT_PATH content scanned for rate-limit signals |
| backoff file → guard | Project-local JSON state read/written each 429 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | rate-limit-backoff.json | mitigate | Validate parsed JSON shape; reset strikeCount if sessionKey mismatch; ignore non-numeric strikeCount |
| T-04-05 | Denial of Service | cooldown sleep | accept | Max cooldown capped at 240s per ADV-02 spec; user-triggered via their own 429 rate limits |
| T-04-06 | Spoofing | sessionKey from path | accept | Transcript path is set by Claude Code runtime; basename dirname is stable session discriminator |
| T-04-07 | Repudiation | rate-limit-log.json | mitigate | Continue appending timestamped entries with strikeCount and cooldownSecs |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `node --test test/gsd-429-guard.test.js` passes unit and integration cases
- `node --test test/*.test.js` passes entire package
- Backoff file is rate-limit-backoff.json, not economy.lock
</verification>

<success_criteria>
- ADV-02 requirement satisfied: 60s → 120s → 240s capped within session, reset on new session
- RATE-02/03/04 behaviors preserved: activate economy, log event, cooldown before exit
</success_criteria>

<output>
Create `.planning/phases/04-advanced-pacing/04-02-SUMMARY.md` when done
</output>
