---
phase: 09-gemini-test-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gsd-gemini-after.test.js
  - test/readme.test.js
autonomous: true
requirements:
  - TEST-01
  - DOCS-02
must_haves:
  truths:
    - "runGeminiAfter with 429 transcript_path creates economy.lock in project cwd"
    - "runGeminiAfter with no transcript_path does not create economy.lock"
    - "readme.test.js REQUIRED tokens include gsd-gemini-after"
    - "Full npm test suite passes (56+ tests)"
  artifacts:
    - path: test/gsd-gemini-after.test.js
      provides: "Runtime integration tests for Gemini AfterAgent 429 recovery"
      contains: "runGeminiAfter"
    - path: test/readme.test.js
      provides: "README documentation guard including gsd-gemini-after"
      contains: "gsd-gemini-after"
  key_links:
    - from: test/gsd-gemini-after.test.js
      to: hooks/gsd-gemini-after.js
      via: "require runGeminiAfter and invoke with transcript_path + cwd"
      pattern: "transcript_path"
    - from: test/gsd-gemini-after.test.js
      to: hooks/gsd-429-guard.js
      via: "runGuard called internally on 429 scan"
      pattern: "economy\\.lock"
---

<objective>
Add missing `gsd-gemini-after` runtime integration test (TEST-01) and extend README doc guard (DOCS-02).

Purpose: Close v1.1 MULTI-01 partial gap with parity to Codex guard tests; no hook or installer changes unless tests expose a bug.
Output: `test/gsd-gemini-after.test.js`, updated `test/readme.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-gemini-test-hardening/09-CONTEXT.md
@test/gsd-codex-429-guard.test.js
@hooks/gsd-gemini-after.js
@test/readme.test.js
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: gsd-gemini-after integration tests</name>
  <files>test/gsd-gemini-after.test.js</files>
  <action>
    Create `test/gsd-gemini-after.test.js` mirroring `test/gsd-codex-429-guard.test.js` structure:

    1. `const { runGeminiAfter } = require('../hooks/gsd-gemini-after');`

    2. **Test: 429 transcript creates economy.lock**
       - `mkdtemp` isolated project dir with `.planning/config.json` (`{}`)
       - Write transcript at `{tmp}/transcripts/gemini-sess/agent.jsonl` with body `error 429 rate limit`
       - Set `GSD_BACKOFF_TEST_MS=10` for fast cooldown
       - Call `await runGeminiAfter({ cwd: tmpDir, transcript_path: transcriptPath, session_id: 'gemini-sess' })`
       - Assert `fs.existsSync(path.join(tmpDir, '.planning', 'economy.lock'))`
       - Restore env and `fs.rmSync` tmpDir in `finally`

    3. **Test: no transcript_path is no-op**
       - Fresh tmpDir, call `await runGeminiAfter({ cwd: tmpDir })` (no transcript_path)
       - Assert economy.lock does NOT exist

    **Important:** Gemini uses `transcript_path` — do NOT use Codex's `agent_transcript_path` field name.

    Do not modify `hooks/gsd-gemini-after.js` unless a test failure proves a real bug.
  </action>
  <verify>
    <automated>node --test test/gsd-gemini-after.test.js</automated>
  </verify>
  <done>Both tests pass; economy.lock created only on 429 path</done>
</task>

<task type="auto">
  <name>Task 2: Extend readme.test.js token guard</name>
  <files>test/readme.test.js</files>
  <action>
    Add `'gsd-gemini-after'` to the `REQUIRED` string array in `test/readme.test.js`.

    Place it immediately after `'gsd-gemini-before'` for readability.

  README already documents `gsd-gemini-after` in the install table and hook reference — no README edit required unless the token is genuinely missing (in which case add it to README, not remove from test).
  </action>
  <verify>
    <automated>node --test test/readme.test.js</automated>
  </verify>
  <done>readme.test.js passes with gsd-gemini-after in REQUIRED list</done>
</task>

<task type="auto">
  <name>Task 3: Full suite verification</name>
  <files></files>
  <action>
    Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All tests pass (expect 58 total: +2 gemini-after tests)</done>
</task>

</tasks>

<verification>
## Phase 9 Success Criteria (from ROADMAP)

1. ✓ `test/gsd-gemini-after.test.js` exists — 429 creates economy.lock
2. ✓ No-transcript no-op covered
3. ✓ `readme.test.js` includes `gsd-gemini-after`
4. ✓ `npm test` green

## Requirements

- **TEST-01:** satisfied when Task 1 verify passes
- **DOCS-02:** satisfied when Task 2 verify passes
</verification>

<success_criteria>
- [ ] `test/gsd-gemini-after.test.js` committed with 2 passing tests
- [ ] `test/readme.test.js` guards `gsd-gemini-after`
- [ ] `npm test` full suite passes
- [ ] `09-01-SUMMARY.md` written after execution
</success_criteria>
