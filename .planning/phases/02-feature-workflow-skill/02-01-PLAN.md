---
phase: 02-feature-workflow-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/gsd-feature/SKILL.md
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-05
  - FEAT-06

must_haves:
  truths:
    - "Invoking /gsd-feature with any description string starts a 3-step workflow without requiring ROADMAP.md to exist"
    - "After discuss step, CONTEXT.md exists at .planning/features/<slug>/CONTEXT.md with user answers in <decisions> section"
    - "After plan step, PLAN.md exists at .planning/features/<slug>/PLAN.md, created inline — no researcher or plan-checker subagent spawned"
    - "Execute+verify step completes in one agent pass; no separate verifier spawn"
    - "Economy settings (budget model, max_concurrent_agents:2, disabled code_review/nyquist/plan_check/verifier) are applied per-invocation in the skill context only — .planning/config.json is never written"
    - "No file write ever touches ROADMAP.md, STATE.md, or any .planning/phases/ path"
    - "Slug derived from feature description is sanitized: [a-z0-9-] only, max 60 chars, rejects '..' and '/'"
  artifacts:
    - path: ".claude/skills/gsd-feature/SKILL.md"
      provides: "gsd-feature skill — 3-step feature workflow"
      contains: "name: gsd-feature"
    - path: ".claude/skills/gsd-feature/SKILL.md"
      provides: "STRIDE threat mitigations for slug injection and path traversal"
      contains: "<security_notes>"
  key_links:
    - from: ".claude/skills/gsd-feature/SKILL.md"
      to: ".planning/economy.lock"
      via: "Bash check in Step 1 bootstrap"
      pattern: "economy\\.lock"
    - from: ".claude/skills/gsd-feature/SKILL.md"
      to: ".planning/features/{slug}/CONTEXT.md"
      via: "Write tool in discuss step"
      pattern: "\\.planning/features/"
---

<objective>
Create `.claude/skills/gsd-feature/SKILL.md` — a Claude Code skill implementing the full 3-step lightweight feature workflow: discuss → plan → execute+verify.

Purpose: Users can develop and ship isolated features without a milestone or ROADMAP.md. Economy settings are applied per-invocation so the feature workflow runs lean (budget model, 2 concurrent agents, no optional pipeline steps) without permanently altering project config. Per FEAT-01 through FEAT-06.

Output: Single skill file at `.claude/skills/gsd-feature/SKILL.md` containing frontmatter, `<objective>`, `<execution_context>`, `<context>`, `<process>` (3 steps), `<notes>`, and `<security_notes>` blocks.
</objective>

<execution_context>
Workflow files are loaded on-demand in the `<process>` section below — not upfront.
Do not pre-load any workflow files before reading the mode routing instructions.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-feature-workflow-skill/02-CONTEXT.md
@.planning/phases/02-feature-workflow-skill/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/skills/gsd-feature/SKILL.md</name>

  <read_first>
  - `C:\Users\Ivan\.claude\skills\gsd-quick\SKILL.md` — canonical SKILL.md format: frontmatter structure, `<objective>` prose style, `<execution_context>` inline variant, `<context>` with $ARGUMENTS, `<process>` argument-parsing header, slug sanitization rule, `<security_notes>` section. Read lines 1-174.
  - `C:\Users\Ivan\.claude\skills\gsd-plan-execute-all\SKILL.md` — multi-step `<process>` conventions: `## Step N — Name` headings, `---` horizontal separators, conditional `**If X:** ...` branches, `AskUserQuestion` `>` blockquote + bold bullet options pattern, banner format, `handle_blocker` section. Read lines 1-340.
  - `.planning/phases/02-feature-workflow-skill/02-PATTERNS.md` — all extracted patterns including CONTEXT.md XML-section structure, economy lock bash check, banner template, and per-invocation budget settings specification.
  - `hooks/gsd-economy.js` lines 1-15 — confirms `lockPath = path.join(planningDir, 'economy.lock')` so the bash check in the skill uses `.planning/economy.lock`.
  </read_first>

  <action>
  Create directory `.claude/skills/gsd-feature/` then write `SKILL.md` with the following exact structure and content.

  **Frontmatter** (per PATTERNS.md frontmatter pattern):

  ```
  name: gsd-feature
  description: "Develop and ship an isolated feature without a milestone or ROADMAP.md"
  argument-hint: "<feature description> [--economy]"
  allowed-tools: Read, Write, Edit, Glob, Grep, Bash, Agent, AskUserQuestion
  ```

  **`<objective>` block** — state:
  - Artifacts live under `.planning/features/<slug>/` — ROADMAP.md, STATE.md, and `.planning/phases/` are NEVER touched
  - Pipeline stages skipped: researcher, plan-checker, verifier, code-review
  - Per-invocation budget settings: model_profile:budget, max_concurrent_agents:2, code_review:false, nyquist_validation:false, plan_check:false, verifier:false — these are applied as inline agent context instructions, NOT written to `.planning/config.json`
  - `--economy` flag: re-check `.planning/economy.lock` presence; if already active, log "Economy already active" and continue; if not active, note per-invocation budget settings apply regardless

  **`<execution_context>` block** — use Style B (inline):
  ```
  Workflow files are loaded on-demand in the <process> section below — not upfront.
  Do not pre-load any workflow files before reading the mode routing instructions.
  ```

  **`<context>` block**:
  ```
  $ARGUMENTS

  Feature slug is derived from $ARGUMENTS (or the feature description portion of it).
  Slug sanitization: strip chars not matching [a-z0-9-], lowercase, replace spaces/special chars with "-", max 60 chars, reject ".." and "/".
  ```

  **`<process>` block** — structured as 3 steps plus handle_blocker:

  ---

  **Step 1 — Bootstrap**

  Parse `$ARGUMENTS`: strip leading/trailing whitespace. If `$ARGUMENTS` contains `--economy`, set `ECONOMY_FLAG=true` and remove the flag from the description string; otherwise `ECONOMY_FLAG=false`.

  Derive `DESCRIPTION` from the remaining argument text. If `DESCRIPTION` is empty, output:
  ```
  Usage: /gsd-feature "<feature description>"
  Example: /gsd-feature "add dark mode toggle to settings page"
  ```
  and stop.

  Derive `SLUG` from `DESCRIPTION`:
  - Lowercase entire string
  - Replace any character not in `[a-z0-9]` with `-`
  - Collapse multiple consecutive `-` into one
  - Strip leading and trailing `-`
  - Truncate to 60 chars
  - If result contains `..` or `/`, output "Invalid feature description — slug cannot contain path traversal." and stop
  - If result is empty after sanitization, output "Invalid feature description — could not derive a valid slug." and stop

  Check economy state:
  ```bash
  ECONOMY_ACTIVE=false
  if [ -f ".planning/economy.lock" ]; then ECONOMY_ACTIVE=true; fi
  ```

  Set `FEATURE_DIR=.planning/features/${SLUG}`

  Check if feature already exists:
  ```bash
  if [ -d "${FEATURE_DIR}" ]; then echo "exists"; fi
  ```
  If exists: display "Feature '${SLUG}' already exists at ${FEATURE_DIR}. Use /gsd-feature resume ${SLUG} to continue." and stop.

  Create the feature directory:
  ```bash
  mkdir -p "${FEATURE_DIR}"
  ```

  Display startup banner:
  ```
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   GSD ► FEATURE: {DESCRIPTION truncated to 50 chars}
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Slug:    {SLUG}
   Folder:  .planning/features/{SLUG}/
   Economy: {ECONOMY_ACTIVE==true ? "on (economy.lock present)" : "off (per-invocation budget settings apply)"}
  ```

  Print per-invocation budget notice (always, regardless of ECONOMY_ACTIVE):
  ```
  ◆ Per-invocation settings: model_profile=budget, max_concurrent_agents=2,
    code_review=false, nyquist_validation=false, plan_check=false, verifier=false
    (not written to .planning/config.json — these settings apply to this feature run only)
  ```

  ---

  **Step 2 — Discuss**

  Print `◆ Step 2: Discuss — gathering feature context`

  Generate 2-3 targeted clarifying questions based on `DESCRIPTION`. Questions must focus on:
  1. **Scope / acceptance** — What does "done" look like? What is the single most important behavior?
  2. **Constraints / stack** — Any specific library, pattern, or file this must follow or avoid?
  3. **Edge cases** (optional, only if the description is ambiguous) — What should happen when X fails or X is missing?

  Use `AskUserQuestion` to ask all questions in a single prompt. Format:

  ```
  > Feature: {DESCRIPTION}
  >
  > Before planning, I need a few details:
  >
  > 1. {Question about scope/acceptance}
  > 2. {Question about constraints/stack}
  > [3. {Question about edge cases — only if warranted}]
  ```

  Capture the user's answers. If the user's response is empty or "skip", proceed with defaults (derive best-guess answers from `DESCRIPTION` alone and note "User skipped discuss — defaults applied").

  Write `${FEATURE_DIR}/CONTEXT.md` using the XML-section structure:

  ```markdown
  # Feature: {DESCRIPTION} — Context

  **Slug:** {SLUG}
  **Gathered:** {today's date in YYYY-MM-DD}
  **Status:** Ready for planning

  <domain>
  ## Feature Scope

  {DESCRIPTION — full description as provided}

  </domain>

  <decisions>
  ## Implementation Decisions

  {User answers mapped to decisions:
  - Acceptance criterion: {answer to Q1}
  - Stack/constraints: {answer to Q2}
  - Edge cases: {answer to Q3 if asked, otherwise "N/A — not raised during discuss"}}

  </decisions>

  <code_context>
  ## Codebase Context

  {Scan the project root for relevant files related to DESCRIPTION using Glob/Grep.
  List 3-5 most relevant existing files with one-line description of each.
  If nothing relevant found, write "No existing code found for this feature domain."}

  </code_context>

  <specifics>
  ## Acceptance Criteria

  {Derive 3-5 numbered acceptance criteria from the discuss answers and DESCRIPTION.
  Each criterion must be a testable, observable behavior.}

  </specifics>

  <deferred>
  ## Deferred Ideas

  None.

  </deferred>
  ```

  Print `◆ CONTEXT.md written to ${FEATURE_DIR}/CONTEXT.md`

  ---

  **Step 3 — Plan**

  Print `◆ Step 3: Plan — creating feature plan from CONTEXT.md`

  Read `${FEATURE_DIR}/CONTEXT.md`.

  Produce a `PLAN.md` inline (no researcher subagent, no plan-checker subagent). The PLAN.md format:

  ```markdown
  # Feature Plan: {DESCRIPTION}

  **Slug:** {SLUG}
  **Created:** {today's date}
  **Economy:** per-invocation (budget model, 2 agents, no optional pipeline steps)

  ## Objective

  {1-2 sentences: what the feature does and why it matters}

  ## Tasks

  ### Task 1: {Action-oriented name}

  **Files:** {exact file paths to create or modify}
  **Action:** {Specific implementation steps}
  **Verify:** {Command or observable behavior that proves task is complete}
  **Done:** {Acceptance criterion from CONTEXT.md that this task satisfies}

  ### Task 2: {Action-oriented name}

  [same structure]

  [Task 3 if needed — maximum 3 tasks total]

  ## Success Criteria

  {Copy the numbered acceptance criteria from CONTEXT.md `<specifics>` section verbatim}

  ## Artifacts

  - {List every file this plan creates or modifies}
  ```

  Keep the plan to 2-3 tasks. Each task must be completable by Claude autonomously.

  Write the plan to `${FEATURE_DIR}/PLAN.md`.

  Print `◆ PLAN.md written to ${FEATURE_DIR}/PLAN.md`

  Ask the user to review before execution:

  ```
  > Feature plan created at ${FEATURE_DIR}/PLAN.md
  >
  > Ready to execute?
  ```

  Options:
  - **"Execute"** — continue to Step 4
  - **"Edit plan first"** — pause; user edits PLAN.md manually; after user signals "done editing", continue to Step 4
  - **"Stop"** — print summary and exit

  ---

  **Step 4 — Execute + Verify**

  Print `◆ Step 4: Execute + Verify — running tasks inline`

  Read `${FEATURE_DIR}/PLAN.md` to extract the task list.

  Execute each task in sequence. For each task:
  - Print `◆ Task {N}: {task name}`
  - Use Read/Write/Edit/Bash/Glob/Grep as needed to implement the task
  - After completing, print `◆ Task {N} done`

  After all tasks complete, perform **inline self-verification** (no separate verifier subagent):

  1. Re-read `${FEATURE_DIR}/CONTEXT.md` `<specifics>` section (the acceptance criteria)
  2. For each criterion, verify it is satisfied:
     - Run the verify command from PLAN.md if one was given
     - Otherwise inspect the created/modified files directly
  3. Build a verification table:

  ```
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   GSD ► FEATURE: {SLUG} ▸ VERIFICATION
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Criterion 1: {text} — PASS / FAIL
  Criterion 2: {text} — PASS / FAIL
  ...

  Overall: PASS / FAIL ({N} of {M} criteria met)
  ```

  Write a `SUMMARY.md` to `${FEATURE_DIR}/SUMMARY.md`:

  ```markdown
  # Feature Summary: {DESCRIPTION}

  **Slug:** {SLUG}
  **Completed:** {today's date}
  **Status:** {complete | incomplete}

  ## Files Created/Modified

  {list}

  ## Verification

  {copy the verification table}

  ## Deviations

  {any deviation from PLAN.md, or "None"}
  ```

  Display final banner:

  ```
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   GSD ► FEATURE: {SLUG} ▸ DONE
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Feature:  {DESCRIPTION}
   Artifacts: .planning/features/{SLUG}/
   Status:   {complete | incomplete — see SUMMARY.md for gaps}
  ```

  If any criterion FAILED: ask the user:
  ```
  > {N} verification criterion/criteria failed. How do you want to proceed?
  ```
  Options:
  - **"Retry failed tasks"** — re-run only the tasks whose criteria failed; re-verify after retry
  - **"Accept as-is"** — mark complete with known gaps in SUMMARY.md
  - **"Stop"** — leave SUMMARY.md with incomplete status for manual follow-up

  ---

  **handle_blocker**

  When a step fails unexpectedly:
  ```
  ⚠ Step {N} — {description of failure}
  ```
  Ask:
  ```
  > How do you want to proceed?
  ```
  Options:
  - **"Retry"** — re-run the failed step (limit: 2 retries)
  - **"Skip"** — log and continue to next step
  - **"Stop"** — exit with current SUMMARY.md state

  **HARD CONSTRAINT throughout all steps:** Never write to ROADMAP.md, STATE.md, or any file under `.planning/phases/`. Feature artifacts are isolated to `.planning/features/{SLUG}/` only. If any step would produce output outside this boundary, stop and report the conflict.

  **`<notes>` block**:
  - Feature artifacts live entirely in `.planning/features/<slug>/` — not tracked in ROADMAP.md or STATE.md
  - Per-invocation economy settings are inline instructions to this agent, not disk writes
  - The `--economy` flag is informational; economy.lock presence is the authoritative signal
  - No subagents are spawned for planning, plan-checking, or verification — all work happens inline
  - Slug collision (feature already exists) is reported as an error; use a different description or clean up the old directory

  **`<security_notes>` block**:
  - Slugs from $ARGUMENTS are sanitized before use in file paths: only [a-z0-9-] allowed, max 60 chars, reject ".." and "/"
  - Directory check (`[ -d "${FEATURE_DIR}" ]`) uses the sanitized slug — never the raw $ARGUMENTS string
  - File names from Glob/ls results are sanitized before display: strip non-printable chars and ANSI escape sequences using `name.replace(/[^\x20-\x7E]/g, '').replace(/[\/\\]/g, '')`
  - Artifact content (feature descriptions, task names) rendered as plain text only — never executed or shell-expanded
  - PLAN.md and CONTEXT.md task/field content is read and acted on by this Claude instance only — never passed raw to Bash or another subagent without explicit DATA_START/DATA_END boundaries
  - ROADMAP.md and STATE.md are read-only within this skill — any code path that would write to them must stop and report an error
  </action>

  <verify>
    <automated>grep -c "name: gsd-feature" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "security_notes" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "Step 1" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "Step 2" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "Step 3" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "Step 4" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "economy.lock" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "\.planning/features/" .claude/skills/gsd-feature/SKILL.md</automated>
    <automated>grep -c "ROADMAP" .claude/skills/gsd-feature/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
  - File `.claude/skills/gsd-feature/SKILL.md` exists
  - Frontmatter contains exactly `name: gsd-feature` (grep returns 1)
  - Frontmatter `allowed-tools` contains AskUserQuestion and Agent
  - File contains `<security_notes>` block (grep -c returns >= 1)
  - File contains `## Step 1`, `## Step 2`, `## Step 3`, `## Step 4` headings (one match each)
  - File contains `economy.lock` string (grep returns >= 1) — confirms economy state detection present
  - File contains `.planning/features/` string (grep returns >= 1) — confirms artifact isolation path present
  - File contains `ROADMAP` string only in the hard-constraint prohibition prose — never as a write target
  - File contains `per-invocation` string — confirms budget settings are not written to disk (FEAT-05)
  - File contains `no researcher` or equivalent prohibition against spawning plan-checker/verifier subagents (FEAT-03, FEAT-04)
  - File contains `max_concurrent_agents` reference — confirms per-invocation setting value
  - File does NOT contain `config.json` as a write target in any Step prose
  - The `<process>` block includes slug sanitization logic: `[a-z0-9-]`, `max 60`, reject `..` and `/`
  - CONTEXT.md write in Step 2 uses the XML-section structure: `<domain>`, `<decisions>`, `<code_context>`, `<specifics>`, `<deferred>`
  </acceptance_criteria>

  <done>
  `.claude/skills/gsd-feature/SKILL.md` exists with valid frontmatter, a complete 4-step `<process>` (Bootstrap, Discuss, Plan, Execute+Verify), economy lock detection, per-invocation budget note, hard constraint against writing ROADMAP.md/STATE.md, slug sanitization in argument parsing, and `<security_notes>` section. All FEAT-01 through FEAT-06 requirements are addressed in the skill prose.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| `.claude/skills/gsd-feature/SKILL.md` | skill file | Claude Code skill — `/gsd-feature` command |
| `.planning/features/<slug>/CONTEXT.md` | runtime artifact | Feature context written by the skill during discuss step |
| `.planning/features/<slug>/PLAN.md` | runtime artifact | Feature plan written by the skill during plan step |
| `.planning/features/<slug>/SUMMARY.md` | runtime artifact | Execution + verification summary written at end of execute step |

Note: The three `.planning/features/` artifacts are produced at skill runtime (when the user invokes `/gsd-feature`), not during this plan execution. The only file produced by executing this plan is `.claude/skills/gsd-feature/SKILL.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS → slug → file path | User-supplied feature description crosses into filesystem path construction |
| PLAN.md content → task execution | Skill-written plan content is re-read and acted on by the same Claude instance |
| Glob/ls output → display | Filesystem names are read and displayed to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Slug derivation in Step 1 | mitigate | Strip all chars not matching `[a-z0-9-]`; reject if result contains `..` or `/`; max 60 chars enforced before any file path construction |
| T-02-02 | Elevation of Privilege | ROADMAP.md / STATE.md write path | mitigate | Hard constraint in every step: any code path that would write outside `.planning/features/{SLUG}/` must stop and report an error; stated explicitly in `<security_notes>` |
| T-02-03 | Information Disclosure | Glob/ls directory names displayed | mitigate | Sanitize names with `replace(/[^\x20-\x7E]/g,'').replace(/[\/\\]/g,'')` before display — prevents ANSI injection and path traversal in rendered output |
| T-02-04 | Tampering | PLAN.md content re-read at execute step | accept | PLAN.md is written by this same skill instance in Step 3 and immediately re-read in Step 4; no external process modifies it between writes; content is task prose, not shell commands |
| T-02-05 | Spoofing | economy.lock absence used as "economy off" signal | accept | Lock file is an advisory signal only; per-invocation budget settings apply regardless of lock state; no security decision depends on lock presence alone |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages — skill is a pure markdown file; no package manager invocations |
</threat_model>

<verification>
After executing this plan:

1. File exists: `.claude/skills/gsd-feature/SKILL.md`
2. Frontmatter parses cleanly (no YAML syntax errors): `grep -A5 "^---" .claude/skills/gsd-feature/SKILL.md`
3. All 6 requirements covered:
   - FEAT-01: Step 1 bootstrap works with no ROADMAP.md — skill has no `require roadmap` check
   - FEAT-02: Step 2 discuss writes CONTEXT.md to `.planning/features/<slug>/`
   - FEAT-03: Step 3 plan is inline — no `Agent(skill="gsd-plan-phase")` call present
   - FEAT-04: Step 4 execute+verify is inline — no `Agent(skill="gsd-execute-phase")` or verifier spawn
   - FEAT-05: Per-invocation budget settings documented in objective and Step 1 — no config.json write
   - FEAT-06: Hard constraint in every step blocks writes outside `.planning/features/{SLUG}/`
4. Security: slug sanitization present in `<process>` Step 1 and `<security_notes>`
</verification>

<success_criteria>
- `.claude/skills/gsd-feature/SKILL.md` exists with valid SKILL.md frontmatter
- `/gsd-feature "some description"` would start the workflow per the `<process>` definition with no ROADMAP.md dependency
- Feature artifacts are written to `.planning/features/<slug>/` only
- Economy settings appear as per-invocation inline instructions, not as `.planning/config.json` mutations
- All FEAT-01 through FEAT-06 requirements addressed in skill prose
</success_criteria>

<output>
Create `.planning/phases/02-feature-workflow-skill/02-01-SUMMARY.md` when done.
</output>
